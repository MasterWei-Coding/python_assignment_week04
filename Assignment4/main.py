import string_operations
import string_operations as so
sample_string = "hello World"

print(original_string := sample_string)  
print(so.reverse_string(sample_string))
print(so.capitalize_string(sample_string))
print(so.lowercase_string(sample_string))
print(so.uppercase_string(sample_string))